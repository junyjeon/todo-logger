# 배포

```bash
# 1. GitHub CLI 인증
gh auth login

# 2. 저장소 생성 및 배포
cd /home/jun/.claude/todo-logger
gh repo create todo-logger \
  --public \
  --source=. \
  --description="🤖 Persistent Task History for AI-Driven Development" \
  --push

# 3. 토픽 추가 (GitHub 웹에서)
# Settings → About → Topics: claude-code, ai, task-management, productivity, logging

# 4. 릴리스 생성
git tag -a v1.0.0 -m "Initial release: Todo-Logger v1.0"
git push origin v1.0.0

# 5. GitHub Release 페이지에서 릴리스 노트 작성
gh release create v1.0.0 --title "v1.0.0 - Initial Release" --notes "첫 공개 릴리스"
```

## 자동 배포 스크립트

```bash
# 인터랙티브 배포
./deploy-to-github-interactive.sh

# 자동 배포
./deploy-to-github.sh
```
